# Production dependencies - Optimized for production deployment
# Minimal set for production with only essential packages

# Include full dependencies but exclude development tools
-r requirements-full.txt

# Production-specific optimizations
# Use specific versions for stability
numpy==1.24.3
pandas==2.0.3
torch==2.0.1
scikit-learn==1.3.0
