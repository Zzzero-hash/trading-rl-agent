# CI-specific requirements - Optimized for continuous integration
# Minimal dependencies for fast CI builds

# Include core dependencies
-r requirements-core.txt

# Testing framework (minimal set for CI)
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0
pytest-mock>=3.11.0,<4.0.0
pytest-xdist>=3.3.0,<4.0.0

# Code quality (essential for CI)
black>=23.7.0,<24.0.0
ruff>=0.0.284,<1.0.0
mypy>=1.5.0,<2.0.0

# Security scanning
bandit>=1.7.5,<2.0.0
safety>=2.3.0,<3.0.0

# Coverage reporting
coverage>=7.2.0,<8.0.0

# Optional: Include ML dependencies for full CI testing
# Uncomment for comprehensive CI testing
# -r requirements-ml.txt
